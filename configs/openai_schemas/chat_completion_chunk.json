{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "OpenAI /v1/chat/completions streaming chunk",
  "description": "Structural envelope for a single SSE chunk in a streaming chat completion. Does not assert per-chunk content — only protocol fields.",
  "type": "object",
  "required": ["id", "object", "choices"],
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "pattern": "^(chatcmpl-|chat-)"
    },
    "object": {
      "type": "string",
      "const": "chat.completion.chunk"
    },
    "created": {
      "type": "integer",
      "minimum": 0
    },
    "model": {
      "type": "string"
    },
    "choices": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["index", "delta"],
        "properties": {
          "index": {"type": "integer", "minimum": 0},
          "delta": {
            "type": "object",
            "properties": {
              "role": {"type": "string", "enum": ["assistant", "system", "user", "tool", "function"]},
              "content": {"type": ["string", "null"]}
            }
          },
          "finish_reason": {
            "type": ["string", "null"],
            "enum": ["stop", "length", "content_filter", "tool_calls", "function_call", null]
          }
        }
      }
    },
    "usage": {
      "type": ["object", "null"],
      "properties": {
        "prompt_tokens": {"type": "integer", "minimum": 0},
        "completion_tokens": {"type": "integer", "minimum": 0},
        "total_tokens": {"type": "integer", "minimum": 0}
      }
    }
  }
}
